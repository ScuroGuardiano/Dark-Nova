import Planet from "../../db/models/planet";

export default class PlanetBuilder {
    constructor(private planet: Planet) {} 
    public build(buildingName: string) {
        
    }
    public destroy(buildingName: string) {

    }
}
